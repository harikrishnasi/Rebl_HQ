{
  "name": "rebl-hq",
  "type": "module",
  "version": "3.0.0",
  "private": true,
  "description": "REBL HQ v3 — single-user founder dashboard. Supabase-backed: Home, Leads, Tasks, Finance, Documents, Journal, with a global company tag filter.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dompurify": "^3.1.6",
    "marked": "^14.1.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
